基础信息：
        智能标签选取：商家信息->标签提取(LDA)
        用户资料（年龄，性别，地址）冷启动
        用户搜索信息->关键词提取->用户分类
        用户购买，收藏信息->关键词提取->根据物品标签分类
1.用户画像需要迭代，目的有两个：
    更加精细化刻画用户的兴趣，也是一个探索问题；
    防止陷入用户短期兴趣不能自拔，还是一个探索问题。
2.用户画像冷启动
    用于冷启动选择的标签集合有限、互相独立、覆盖内容广；
    标签索引的内容库要单独准备，保证高质量；
    为每一个用户都保存 bandit 算法参数，互相不共享。